package chess.model;


import chess.model.Move;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;


/*
class MoveTest {

    @Test
    void parsesPositionsFromInput(){
        Move move = new Move("e2-e4");
        int vertical = move.start.getVertical();
        int horizontal = move.start.getHorizont();
        int verticalEnd = move.end.getVertical();
        int horizontalEnd= move.end.getHorizont();
        Assertions.assertEquals(vertical, 1);
        Assertions.assertEquals(horizontal, 4);
        Assertions.assertEquals(verticalEnd, 2);
        Assertions.assertEquals(horizontalEnd, 4);
    }


}*/
